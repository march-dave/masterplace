// "use strict";
//
// angular
//     .module("masterplaceApp")
//     .controller("mainCtrl", mainCtrl)
//
// function mainCtrl($anchorScroll, $timeout, Application, $scope, $window, $state, $location, toaster) {
//
// (function() {
//     if ($scope.currentUser && !store.get("id_token") && !store.get("googleAPIAccess") && !store.get("currentUserMId")) {
//         logoutUserFromLocalDB();
//         $scope.currentUser = null;
//         $state.go('dashboard');
//         $window.location.reload();
//     }
// }());
